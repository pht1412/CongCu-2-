#! sqlserver-sql_data-persistentVolumeClaim.yaml
# Generated code, do not edit
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: sqlserver-sql-data
    namespace: lehongphat
    labels:
        com.docker.compose.service: sqlserver
        com.docker.compose.volume: sql_data
spec:
    accessModes:
        # TODO would need to check which services use this volume
        - ReadWriteOnce
    volumeMode: Filesystem
    resources:
        requests:
            storage: 100Mi
